# Global-Micro-Help-Demo
A lightweight, offline-capable Progressive Web App prototype for posting and claiming tiny needs or offers; client-only demo that stores data in localStorage and uses a service worker for offline caching.
